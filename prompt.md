## Semantic Versioning Validator: self buildingg tutorial

Ok. Now can you pllease

#### create a python script

- [ ] that uses both these regular expressions too validate semantic versioning numbers..
- [ ] The script will be a command line tool/utility that will use argparse and
- [ ]  a basic interactive mode,
- [ ] usage string, etc.
- [ ] The script will be the foundation of a simple app that will serve as

#### a tool to publis accoriding too the rules of SemVer

as described in the pages we just saw/buily.
I will organize the project folder and create a git repo etc.
(as well as adding the html file we created and the reference docs)

### WHat i need you to do

1. create main.py : the file i just described that is a cli tool that:

- [ ] validates version numsbers as pass/fail semver numbers and
- [ ]  returns a json string with pass/fail and
- [ ] parses the major: x, minor: y, and patch: z , into json properties
  - [ ] for valid semver numbers,.
  - [ ]  for failing numbers it should instead return fail and
  - [ ]  the causes of invalidation.
1. ***readme.md file***

- [ ] describing the app
1. ***agent.md***

- [ ] and gemini.md with
- [ ] instructions for agents
1. ***MOST IMportant***

- [ ] INSTUCTIONS.md,
- [ ] this file list the step by steps neccesary  
- [ ] to publish the app as a version 0.x and
- [ ] ultimatly a 1.0.0 .
1. ***The steps must include***

- [ ] setting up an api endpoint for the
- [ ] purpose of semver requirement
- [ ] the api should use python and fastapi and
- [ ] should take curl requests with a semver numvber canditae and
- [ ] return a json string jsut like the cli app.
1. ***instuctions for puplishing the app***

- [ ] too pipy or equivelent and
- [ ] compiling too an executable(for the cli tool at least) so it
- [ ]  can run without python ___.py

## ***this will serve as a introduction***

- [ ] too publlish and semantic versioning and
- [ ] it takes the form of an version number validator app/api.
        `as i said`
- [ ] i will provide the base url for the api and wil
- [ ] copy the files code from canvas too the folder.repo

> but i need the four mentioned files generated
>>
>> - main.py and
>> - REAdme.md,
>> - agent.md,
>> - gemini.md and the
>> - INSTRUCTIONS.md with the steps to take the app from cli pyhthon
>> tool
> tooa published SEmantic version python package with API to satiosfy
> versioning rules
